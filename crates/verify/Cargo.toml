[package]
name = "shelliq-verify"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Command line scanning, flag bundle decomposition, and index-backed option checking"

[dependencies]
anyhow = "1.0.104"
shelliq-index = { version = "0.1.0-alpha.1", path = "../index" }

[dev-dependencies]
shelliq-harvest = { path = "../harvest" }
